@model IEnumerable<WebApplication1.Application.ViewModels.ItemViewModel>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Modelo</th>
            <th>Categoria</th>
            <th>Patrimônio</th>
            <th>Número de Série</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Any())
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Modelo</td>
                    <td>@item.Categoria</td>
                    <td>@item.Patrimonio</td>
                    <td>@item.NumeroSerie</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Editar</a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Detalhes</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Excluir</a>
                        <a class="btn btn-sm btn-secondary" href="/Manutencao?itemId=@item.Id">Manutenções</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5" class="text-center">Nenhum item encontrado.</td>
            </tr>
        }
    </tbody>
</table>